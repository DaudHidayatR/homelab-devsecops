apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: openbao

resources:
  - helmrepository.yaml
  - helmrelease.yaml

configMapGenerator:
  - name: openbao-values
    files:
      - values.yaml
