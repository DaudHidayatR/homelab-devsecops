apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - helmrepository.yaml
  - helmrelease.yaml
  - default-deny-ingress.yaml
  - stores

# Store resources remain Flux-owned. They may report not ready until OpenBao is
# initialized, unsealed, and seeded; ESO retries reconciliation automatically.
