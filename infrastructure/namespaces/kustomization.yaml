apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - demo.yaml
  - openbao.yaml
  - istio-system.yaml
  - headlamp.yaml
  - ci-rbac.yaml

patches:
  - path: patches/common-labels.yaml
    target:
      kind: Namespace
