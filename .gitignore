.mcp.json
# Local MCP memory contains assistant runtime state
.mcp-memory.json
# Environment configuration (contains deployment constants; example file is tracked)
config.env
config.env.dev
config.env.prod
.env
# Security scan reports (may contain secret fragments or vulnerability data)
trivy-report.*
trivy-rendered.*
grype-report.*
gitleaks-report.*
semgrep-report.*
checkov-report.*
sbom-*.json
.rendered-manifests.yaml
raw/*
plans/*
docs/openbao-integration.md
.commandcode

# Runtime backups and OpenBao init output may contain root tokens, unseal keys, Tailscale machine keys, and OAuth secrets
.runtime-backups/
openbao-init.json
.runtime/
